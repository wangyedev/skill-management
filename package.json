{
  "name": "skillscan-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-client skill discovery and management tool",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "dev:cli": "npm run build -w packages/cli && node packages/cli/dist/bin.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "MIT"
}
